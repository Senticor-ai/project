# Rate limiting zones — include this file at the http {} level.
# Writes: 100 requests/minute per IP (burst allows short spikes).
limit_req_zone $binary_remote_addr zone=writes:10m rate=100r/m;

# Auth: 10 requests/minute per IP (login/register brute-force protection).
limit_req_zone $binary_remote_addr zone=auth:10m rate=10r/m;
