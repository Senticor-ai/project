apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
spec:
  template:
    spec:
      containers:
        - name: worker
          volumeMounts:
            - name: pubsub-sa
              mountPath: /etc/gcp
              readOnly: true
      volumes:
        - name: pubsub-sa
          secret:
            secretName: pubsub-sa
